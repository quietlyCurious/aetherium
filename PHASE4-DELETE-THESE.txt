Phase 4 — files to DELETE after unzipping
=========================================

The two manual-layout canvases were merged into one shared canvas, so
their old files are gone. Delete these two by hand:

  src/operator/properties/PropertyLayoutCanvas.jsx   -> canvas/ManualLayoutCanvas.jsx
  src/operator/relatedAssets/CardsLayoutCanvas.jsx      + properties/PropertyTileCanvas.jsx
                                                        + relatedAssets/AssetCardCanvas.jsx

  git rm src/operator/properties/PropertyLayoutCanvas.jsx \
  git rm src/operator/relatedAssets/CardsLayoutCanvas.jsx

Also worth checking, from phase 3: `git status` should show NO untracked
files under src/operator/ once you've committed. Phase 3's commit picked
up only the deletions, so if the new modules are still untracked, run
`git add -A` before committing this phase.
